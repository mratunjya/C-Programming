#include <stdio.h>

int main()
{
	int x, y, SUM;
	printf("Enter Fist Number:\f ");
	scanf("%d", &x);
	printf("Enter Second Number: ");
	scanf("%d", &y);
	SUM = x + y;
	printf("SUM = %d", SUM);
	return(0);
}
